name: NuGet CI & Publish

# Workflow reutilizable. Invocar desde cada repositorio SDK con:
#
#   jobs:
#     ci-publish:
#       uses: APS-Framework/.github/.github/workflows/nuget-ci-publish.yml@main
#       with:
#         release_type: ${{ inputs.release_type || '' }}
#         packages: ${{ inputs.packages || '' }}   # uno o varios paquetes separados por comas
#       secrets: inherit

on:
  workflow_call:
    inputs:
      release_type:
        description: '"rc", "stable" o "" (solo build+test)'
        required: false
        type: string
        default: ''
      packages:
        description: 'One or more PackageIds separated by commas (e.g. APS.Foo or APS.Foo,APS.Bar). Empty = single package repo.'
        required: false
        type: string
        default: ''
    secrets:
      APS_NUGET_TOKEN:
        description: >-
          Classic PAT con scope read:packages. Otorga acceso de lectura a todos los feeds
          NuGet privados de las organizaciones corporativas. Cada fuente corporativa en el
          nuget.config referencia esta variable con %APS_NUGET_TOKEN%. Gestionado por el
          administrador de la empresa y compartido como org secret en APS-Framework.
        required: true
      NUGET_PUBLISH_TOKEN:
        description: >-
          PAT con scope write:packages de la organización publicadora. Se usa exclusivamente
          para dotnet nuget push. Para repos de APS-Framework puede ser el mismo token que
          APS_NUGET_TOKEN. Para repos de otras organizaciones debe ser un PAT con
          write:packages sobre su propia org.
        required: true
      NUGET_EXTERNAL_TOKEN:
        description: >-
          PAT opcional para feeds NuGet privados de proveedores externos a la compañía.
          Referenciar en el nuget.config como %NUGET_EXTERNAL_TOKEN%. Si el repositorio no
          consume feeds externos, este secret puede omitirse.
        required: false

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.x
            10.x

      - name: Resolve project paths
        id: resolve
        run: |
          set -euo pipefail
          packages="${{ inputs.packages }}"

          if [ -z "$packages" ]; then
            echo "scoped=false" >> "$GITHUB_OUTPUT"
            echo "lib_args=" >> "$GITHUB_OUTPUT"
            echo "test_args=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          lib_projects=()
          IFS=',' read -ra list <<< "$packages"
          for pkg in "${list[@]}"; do
            pkg="${pkg// /}"
            csproj=$(grep -rl "<PackageId>${pkg}</PackageId>" --include="*.csproj" src/ | head -1 || true)
            if [ -z "$csproj" ]; then
              csproj=$(find src/ -name "${pkg}.csproj" | head -1 || true)
            fi
            if [ -z "$csproj" ]; then
              echo "::error::No se encontró .csproj para PackageId o nombre '${pkg}' bajo src/"
              exit 1
            fi
            lib_projects+=("${csproj#./}")
          done

          test_projects=()
          for lib in "${lib_projects[@]}"; do
            lib_name=$(basename "$lib")
            while IFS= read -r test_csproj; do
              test_projects+=("${test_csproj#./}")
            done < <(grep -rl "$lib_name" --include="*.csproj" . | grep -v "$lib" | sed 's|^\./||' || true)
          done

          unique_libs=$(printf '%s\n' "${lib_projects[@]}" | sort -u | tr '\n' ' ')
          unique_tests=$(printf '%s\n' "${test_projects[@]:-}" | grep -v '^$' | sort -u | tr '\n' ' ' || true)

          echo "scoped=true" >> "$GITHUB_OUTPUT"
          echo "lib_args=${unique_libs% }" >> "$GITHUB_OUTPUT"
          echo "test_args=${unique_tests% }" >> "$GITHUB_OUTPUT"

      - name: Restore
        run: |
          if [ "$SCOPED" = "false" ]; then
            dotnet restore
          else
            for proj in $LIB_ARGS $TEST_ARGS; do dotnet restore "$proj"; done
          fi
        env:
          SCOPED: ${{ steps.resolve.outputs.scoped }}
          LIB_ARGS: ${{ steps.resolve.outputs.lib_args }}
          TEST_ARGS: ${{ steps.resolve.outputs.test_args }}
          APS_NUGET_TOKEN: ${{ secrets.APS_NUGET_TOKEN }}
          NUGET_EXTERNAL_TOKEN: ${{ secrets.NUGET_EXTERNAL_TOKEN }}

      - name: Build
        run: |
          if [ "$SCOPED" = "false" ]; then
            dotnet build --no-restore -c Release
          else
            for proj in $LIB_ARGS $TEST_ARGS; do dotnet build "$proj" --no-restore -c Release; done
          fi
        env:
          SCOPED: ${{ steps.resolve.outputs.scoped }}
          LIB_ARGS: ${{ steps.resolve.outputs.lib_args }}
          TEST_ARGS: ${{ steps.resolve.outputs.test_args }}

      - name: Test
        run: |
          if [ "$SCOPED" = "false" ]; then
            dotnet test --no-build -c Release
          elif [ -n "$TEST_ARGS" ]; then
            for proj in $TEST_ARGS; do dotnet test "$proj" --no-build -c Release; done
          else
            echo "No se encontraron proyectos de test para los paquetes seleccionados."
          fi
        env:
          SCOPED: ${{ steps.resolve.outputs.scoped }}
          TEST_ARGS: ${{ steps.resolve.outputs.test_args }}

  publish:
    name: Pack & Publish
    runs-on: ubuntu-latest
    needs: build
    if: inputs.release_type != ''

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # necesario para consultar todos los tags

      - name: Checkout shared workflow scripts
        uses: actions/checkout@v4
        with:
          repository: APS-Framework/.github
          ref: main
          path: .APS-github

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.x
            10.x

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Restore
        run: dotnet restore
        env:
          APS_NUGET_TOKEN: ${{ secrets.APS_NUGET_TOKEN }}
          NUGET_EXTERNAL_TOKEN: ${{ secrets.NUGET_EXTERNAL_TOKEN }}

      - name: Publicar paquetes
        env:
          APS_NUGET_TOKEN: ${{ secrets.APS_NUGET_TOKEN }}
          NUGET_PUBLISH_TOKEN: ${{ secrets.NUGET_PUBLISH_TOKEN }}
          NUGET_EXTERNAL_TOKEN: ${{ secrets.NUGET_EXTERNAL_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 .APS-github/.github/scripts/nuget_publish.py \
            --release-type "${{ inputs.release_type }}" \
            --packages "${{ inputs.packages }}" \
            --org "${{ github.repository_owner }}"
